* use noop if not configured
* use grpc based ones for grpc transport
* use http based ones for http transport


* change to use env var for service name
* also need to create context with resource

* have env_var to silence logs from /health and /ready paths